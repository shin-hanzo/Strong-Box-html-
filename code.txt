
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Strongbox - Manage passwords </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Vazirmatn:wght@100..900&display=swap');
       
        body {
            font-family: 'Vazirmatn', sans-serif;
            background-color: #f3f4f6;
        }
       
        .password-strength {
            height: 5px;
            transition: all 0.3s;
        }
       
        .password-item {
            transition: all 0.2s;
        }
       
        .password-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
       
        .copy-success {
            animation: fadeOut 2s forwards;
        }
       
        @keyframes fadeOut {
            0% { opacity: 1; }
            100% { opacity: 0; }
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <!-- heather -->
        <header class="mb-8 text-center">
            <div class="flex items-center justify-center mb-4">
                <i class="fas fa-lock-alt text-4xl text-indigo-600 mr-3"></i>
                <h1 class="text-3xl font-bold text-gray-800">Strongbox</h1>
            </div>
            <p class="text-gray-600 max-w-2xl mx-auto"> A safe and simple password management system to keep all your passwords in one safe place </p>
        </header>

        <!-- The main part -->
        <main class="bg-white rounded-xl shadow-lg overflow-hidden">
            <!-- Toolbar -->
            <div class="bg-indigo-600 px-6 py-4 flex flex-wrap items-center justify-between">
                <div class="flex items-center">
                    <button id="addPasswordBtn" class="bg-white text-indigo-600 px-4 py-2 rounded-lg font-medium mr-4 hover:bg-indigo-50 transition-colors">
                        <i class="fas fa-plus mr-2"></i> New Password
                    </button>
                    <div class="relative">
                        <input type="text" id="searchInput" placeholder="Saerch..." class="py-2 px-4 pr-10 rounded-lg border-0 focus:ring-2 focus:ring-indigo-400">
                        <i class="fas fa-search absolute left-3 top-3 text-gray-400"></i>
                    </div>
                </div>
                <div class="mt-4 md:mt-0">
                    <span class="text-white text-sm">Number of codes: <span id="passwordCount" class="font-bold">0</span></span>
                </div>
            </div>

            <!-- Add new password from -->
            <div id="passwordForm" class="px-6 py-6 hidden">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label for="siteName" class="block text-gray-700 mb-2">Site / service name</label>
                        <input type="text" id="siteName" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent">
                    </div>
                    <div>
                        <label for="siteUrl" class="block text-gray-700 mb-2">Site address</label>
                        <input type="url" id="siteUrl" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent">
                    </div>
                    <div>
                        <label for="username" class="block text-gray-700 mb-2">username</label>
                        <input type="text" id="username" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent">
                    </div>
                    <div>
                        <label for="password" class="block text-gray-700 mb-2">Password</label>
                        <div class="relative">
                            <input type="password" id="password" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent">
                            <button id="generatePassword" class="absolute left-3 top-2 text-gray-500 hover:text-indigo-600">
                                <i class="fas fa-random"></i>
                            </button>
                            <button id="togglePassword" class="absolute left-10 top-2 text-gray-500 hover:text-indigo-600">
                                <i class="far fa-eye"></i>
                            </button>
                        </div>
                        <div class="mt-2">
                            <div class="flex justify-between mb-1">
                                <span class="text-xs font-medium text-gray-500">Password strength:</span>
                                <span id="strengthText" class="text-xs font-medium">Weak!
                            </div>
                            <div class="w-full bg-gray-200 rounded-full h-1.5">
                                <div id="passwordStrength" class="password-strength h-1.5 rounded-full bg-red-500 w-1/4"></div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="mt-4">
                    <label for="notes" class="block text-gray-700 mb-2">Notes</label>
                    <textarea id="notes" rows="3" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent"></textarea>
                </div>
                <div class="mt-6 flex justify-end space-x-3">
                    <button id="cancelBtn" class="px-4 py-2 text-gray-600 hover:text-gray-800">Cancellation</button>
                    <button id="savePasswordBtn" class="px-6 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition-colors">ذخیره رمز</button>
                </div>
            </div>

            <!-- Passwords List -->
            <div id="passwordsList" class="divide-y divide-gray-200">
                <!-- Shows Password -->
                <div class="p-4 text-center text-gray-500" id="emptyState">
                    <i class="fas fa-lock-open text-3xl mb-3 text-gray-300"></i>
                    <p>No password added yet.</p>
                </div>
            </div>
        </main>

        <!-- Edit modal -->
        <div id="editModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden z-50">
            <div class="bg-white rounded-lg w-full max-w-md mx-4">
                <div class="px-6 py-4 border-b border-gray-200 flex justify-between items-center">
                    <h3 class="text-lg font-medium text-gray-900">Edit a password</h3>
                    <button id="closeEditModal" class="text-gray-400 hover:text-gray-600">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
                <div class="p-6">
                    <div class="mb-4">
                        <label for="editSiteName" class="block text-gray-700 mb-2">Site name</label>
                        <input type="text" id="editSiteName" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent">
                    </div>
                    <div class="mb-4">
                        <label for="editSiteUrl" class="block text-gray-700 mb-2">address site</label>
                        <input type="url" id="editSiteUrl" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent">
                    </div>
                    <div class="mb-4">
                        <label for="editUsername" class="block text-gray-700 mb-2">User name</label>
                        <input type="text" id="editUsername" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent">
                    </div>
                    <div class="mb-4">
                        <label for="editPassword" class="block text-gray-700 mb-2">Password</label>
                        <div class="relative">
                            <input type="password" id="editPassword" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent">
                            <button id="generateEditPassword" class="absolute left-3 top-2 text-gray-500 hover:text-indigo-600">
                                <i class="fas fa-random"></i>
                            </button>
                            <button id="toggleEditPassword" class="absolute left-10 top-2 text-gray-500 hover:text-indigo-600">
                                <i class="far fa-eye"></i>
                            </button>
                        </div>
                    </div>
                    <div class="mb-4">
                        <label for="editNotes" class="block text-gray-700 mb-2">Note</label>
                        <textarea id="editNotes" rows="3" class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-transparent"></textarea>
                    </div>
                </div>
                <div class="px-6 py-4 border-t border-gray-200 flex justify-end space-x-3">
                    <button id="deletePasswordBtn" class="px-4 py-2 text-red-600 hover:text-red-800 flex items-center">
                        <i class="fas fa-trash-alt mr-2"></i> Trash
                    </button>
                    <button id="updatePasswordBtn" class="px-6 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition-colors">Update Password </button>
                </div>
            </div>
        </div>

        <!-- New password -->
        <div id="generateModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden z-50">
            <div class="bg-white rounded-lg w-full max-w-md mx-4">
                <div class="px-6 py-4 border-b border-gray-200 flex justify-between items-center">
                    <h3 class="text-lg font-medium text-gray-900">Strong password generation</h3>
                    <button id="closeGenerateModal" class="text-gray-400 hover:text-gray-600">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
                <div class="p-6">
                    <div class="mb-4">
                        <div class="flex justify-between items-center mb-2">
                            <label for="generatedPassword" class="text-gray-700">Generated password</label>
                            <div class="flex space-x-2">
                                <button id="copyGeneratedPassword" class="text-indigo-600 hover:text-indigo-800">
                                    <i class="fas fa-copy"></i> copy
                                </button>
                                <button id="refreshPassword" class="text-indigo-600 hover:text-indigo-800">
                                    <i class="fas fa-sync-alt"></i> new
                                </button>
                            </div>
                        </div>
                        <div class="relative">
                            <input type="text" id="generatedPassword" class="w-full px-4 py-2 rounded-lg border border-gray-300 bg-gray-100 font-mono" readonly>
                            <div id="copySuccess" class="absolute left-3 top-2 bg-green-100 text-green-800 text-xs px-2 py-1 rounded hidden copy-success">
                                done copy!
                            </div>
                        </div>
                    </div>
                    <div class="space-y-4">
                        <div>
                            <label for="passwordLength" class="block text-gray-700 mb-2">Password length: <span id="lengthValue">12</span></label>
                            <input type="range" id="passwordLength" min="6" max="32" value="12" class="w-full">
                        </div>
                        <div>
                            <div class="flex items-center">
                                <input type="checkbox" id="includeUppercase" class="rounded text-indigo-600" checked>
                                <label for="includeUppercase" class="mr-2 text-gray-700">capital letters (A-Z)</label>
                            </div>
                        </div>
                        <div>
                            <div class="flex items-center">
                                <input type="checkbox" id="includeLowercase" class="rounded text-indigo-600" checked>
                                <label for="includeLowercase" class="mr-2 text-gray-700">small letters (a-z)</label>
                            </div>
                        </div>
                        <div>
                            <div class="flex items-center">
                                <input type="checkbox" id="includeNumbers" class="rounded text-indigo-600" checked>
                                <label for="includeNumbers" class="mr-2 text-gray-700">num(0-9)</label>
                            </div>
                        </div>
                        <div>
                            <div class="flex items-center">
                                <input type="checkbox" id="includeSymbols" class="rounded text-indigo-600" checked>
                                <label for="includeSymbols" class="mr-2 text-gray-700">Sambols(!@#$%)</label>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="px-6 py-4 border-t border-gray-200 flex justify-end space-x-3">
                    <button id="cancelGeneratePassword" class="px-4 py-2 text-gray-600 hover:text-gray-800">Cancel</button>
                    <button id="useGeneratedPassword" class="px-6 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition-colors">Use</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        // First
        let passwords = [];
        let currentEditId = null;

        // DOM元素
        const passwordForm = document.getElementById('passwordForm');
        const passwordsList = document.getElementById('passwordsList');
        const emptyState = document.getElementById('emptyState');
        const passwordCount = document.getElementById('passwordCount');
        const addPasswordBtn = document.getElementById('addPasswordBtn');
        const cancelBtn = document.getElementById('cancelBtn');
        const savePasswordBtn = document.getElementById('savePasswordBtn');
        const searchInput = document.getElementById('searchInput');
        const editModal = document.getElementById('editModal');
        const closeEditModal = document.getElementById('closeEditModal');
        const deletePasswordBtn = document.getElementById('deletePasswordBtn');
        const updatePasswordBtn = document.getElementById('updatePasswordBtn');
        const generateModal = document.getElementById('generateModal');
        const closeGenerateModal = document.getElementById('closeGenerateModal');
        const generatePasswordBtn = document.getElementById('generatePassword');
        const togglePassword = document.getElementById('togglePassword');
        const passwordInput = document.getElementById('password');
        const passwordStrength = document.getElementById('passwordStrength');
        const strengthText = document.getElementById('strengthText');
        const generateModalBtn = document.getElementById('generatePassword');
        const generatedPassword = document.getElementById('generatedPassword');
        const copyGeneratedPassword = document.getElementById('copyGeneratedPassword');
        const refreshPassword = document.getElementById('refreshPassword');
        const useGeneratedPassword = document.getElementById('useGeneratedPassword');
        const cancelGeneratePassword = document.getElementById('cancelGeneratePassword');
        const passwordLength = document.getElementById('passwordLength');
        const lengthValue = document.getElementById('lengthValue');
        const includeUppercase = document.getElementById('includeUppercase');
        const includeLowercase = document.getElementById('includeLowercase');
        const includeNumbers = document.getElementById('includeNumbers');
        const includeSymbols = document.getElementById('includeSymbols');
        const copySuccess = document.getElementById('copySuccess');
        const generateEditPassword = document.getElementById('generateEditPassword');
        const toggleEditPassword = document.getElementById('toggleEditPassword');
        const editPasswordInput = document.getElementById('editPassword');

        // Event
        addPasswordBtn.addEventListener('click', () => {
            passwordForm.classList.remove('hidden');
            clearForm();
        });

        cancelBtn.addEventListener('click', () => {
            passwordForm.classList.add('hidden');
        });

        savePasswordBtn.addEventListener('click', savePassword);

        searchInput.addEventListener('input', searchPasswords);

        togglePassword.addEventListener('click', () => {
            togglePasswordVisibility(passwordInput, togglePassword);
        });

        passwordInput.addEventListener('input', checkPasswordStrength);

        generatePasswordBtn.addEventListener('click', showGenerateModal);

        copyGeneratedPassword.addEventListener('click', () => {
            copyToClipboard(generatedPassword.value);
            copySuccess.classList.remove('hidden');
            setTimeout(() => {
                copySuccess.classList.add('hidden');
            }, 2000);
        });

        refreshPassword.addEventListener('click', generateNewPassword);

        useGeneratedPassword.addEventListener('click', () => {
            const currentActiveInput = document.activeElement;
            if (currentActiveInput.id === 'password' || currentActiveInput.id === 'editPassword') {
                currentActiveInput.value = generatedPassword.value;
                if (currentActiveInput.id === 'password') {
                    checkPasswordStrength();
                }
            } else {
                passwordInput.value = generatedPassword.value;
                checkPasswordStrength();
            }
            generateModal.classList.add('hidden');
        });

        cancelGeneratePassword.addEventListener('click', () => {
            generateModal.classList.add('hidden');
        });

        passwordLength.addEventListener('input', function() {
            lengthValue.textContent = this.value;
            generateNewPassword();
        });

        includeUppercase.addEventListener('change', generateNewPassword);
        includeLowercase.addEventListener('change', generateNewPassword);
        includeNumbers.addEventListener('change', generateNewPassword);
        includeSymbols.addEventListener('change', generateNewPassword);

        toggleEditPassword.addEventListener('click', () => {
            togglePasswordVisibility(editPasswordInput, toggleEditPassword);
        });

        generateEditPassword.addEventListener('click', () =>